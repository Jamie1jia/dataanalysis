#第一步：调用pandas包
import pandas as pd
import os
# iris_concat = pd.DataFrame()
# # 第二步：读取数据并合并
# for name in os.listdir('./data/'):
#     iris = pd.read_excel('./data/%s'%(name))
#     iris_concat = pd.concat([iris_concat,iris1])
# iris_concat.to_excel('./iris_concat.xlsx')#数据保存路径   